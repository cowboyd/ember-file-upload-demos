import { objectUrl } from '../../../helpers/object-url';
import { module, test } from 'qunit';

module('Unit | Helper | object url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = objectUrl(42);
  assert.ok(result);
});
